<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Siera">
        <activity
            android:name=".admin.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".admin.UserActivity"
            android:exported="false" />
        <activity
            android:name=".admin.ManajemenKegiatanActivity"
            android:exported="false" />
        <activity
            android:name=".admin.DashboardAdminActivity"
            android:exported="false" /> <!-- SPLASH (HALAMAN PERTAMA) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- MAIN -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- REGISTER -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".DashboardActivity"
            android:exported="false" />
    </application>

</manifest>